{
  "name": "deckora",
  "version": "0.1.0",
  "private": true,
  "description": "Deckora - PWA moderna de juegos de cartas clásicos",
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "dev:web": "pnpm --filter @deckora/web dev",
    "dev:server": "pnpm --filter @deckora/server dev",
    "build": "pnpm --filter @deckora/shared build && pnpm --filter @deckora/web build && pnpm --filter @deckora/server build",
    "build:web": "pnpm --filter @deckora/web build",
    "build:server": "pnpm --filter @deckora/server build",
    "start": "pnpm --filter @deckora/server start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": "22.x"
  },
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
