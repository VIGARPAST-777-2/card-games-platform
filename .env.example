# ── Server (Render / local) — NUNCA subir .env real ─────────────────────────
# URL del proyecto (pública)
SUPABASE_URL=https://ntaioerwelnqurhgjdqq.supabase.co

# Solo backend. Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=

# Opcional
NODE_ENV=production
PORT=3000
